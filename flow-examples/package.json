{
  "name": "@oomol/oocana-test",
  "private": true,
  "scripts": {
    "test": "pnpm run query && pnpm run flow && pnpm run cache",
    "cache": "vitest run cache --bail 1",
    "query": "vitest run query --bail 1",
    "flow": "vitest run flow --bail 1",
    "layer": "vitest run layer --bail 1",
    "nonlayer": "vitest run macos --bail 1"
  },
  "dependencies": {
    "@oomol/oocana": "workspace:*",
    "@oomol/oocana-types": "workspace:*"
  },
  "devDependencies": {
    "remitter": "^0.4.6",
    "vitest": "^2.1.9"
  }
}
